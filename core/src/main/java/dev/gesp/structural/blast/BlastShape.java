package dev.gesp.structural.blast;

/**
 * The geometry of a blast's reach. Only spherical for now; the enum leaves room
 * for directional/cylindrical shaped charges later.
 */
public enum BlastShape {
    SPHERE
}
